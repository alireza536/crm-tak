import { BadRequestException,Injectable,NotFoundException } from '@nestjs/common';import { InjectRepository } from '@nestjs/typeorm';import { randomBytes } from 'crypto';import { DataSource,EntityManager,Repository } from 'typeorm';import { Customer } from '../entities/customer.entity';import { CustomerHistory } from '../entities/customer-history.entity';import { FollowUp } from '../entities/follow-up.entity';import { Invoice } from '../entities/invoice.entity';import { Quotation,QuotationStatus } from '../entities/quotation.entity';import { QuotationItem } from '../entities/quotation-item.entity';import { User } from '../entities/user.entity';
type Actor={id:number;role:string}; const STATUSES:QuotationStatus[]=['DRAFT','SENT','APPROVED','REJECTED','EXPIRED'];
@Injectable()
export class QuotationService{
 constructor(@InjectRepository(Quotation)private readonly quotations:Repository<Quotation>,private readonly dataSource:DataSource){}
 async create(data:any,actor:Actor){const values=this.calculate(data);return this.dataSource.transaction(async manager=>{const{customer,user}=await this.owner(manager,data.customerId,data.userId,actor);const quotationNumber=String(data.quotationNumber||this.number('QUO')).trim();if(!quotationNumber)throw new BadRequestException('quotationNumber is required');if(await manager.getRepository(Quotation).exist({where:{quotationNumber}}))throw new BadRequestException('quotationNumber already exists');const entity=manager.getRepository(Quotation).create({customerId:customer.id,userId:user.id,quotationNumber,quotationDate:data.quotationDate?this.validDate(data.quotationDate):new Date(),description:String(data.description||'').trim()||null,...values,status:data.status?this.validStatus(data.status):'DRAFT',validUntil:data.validUntil?this.validDate(data.validUntil):null,items:values.items.map(i=>manager.getRepository(QuotationItem).create(i))});const saved=await manager.getRepository(Quotation).save(entity);await manager.getRepository(FollowUp).save({customerId:customer.id,userId:user.id,type:'QUOTE',title:'Ù¾ÛŒÚ¯ÛŒØ±ÛŒ Ù¾ÛŒØ´â€ŒÙØ§Ú©ØªÙˆØ±',description:`Ù¾ÛŒÚ¯ÛŒØ±ÛŒ ${saved.quotationNumber}`,status:'PENDING',dueDate:saved.validUntil??new Date(Date.now()+3*86400000),completedAt:null});await this.history(manager,customer.id,actor.id,`Quotation ${saved.quotationNumber} created`);return this.one(saved.id,actor,manager)})}
 async all(actor:Actor){await this.expire();return this.query(actor).orderBy('quotation.createdAt','DESC').getMany()}
 async byCustomer(customerId:number,actor:Actor){const customer=await this.dataSource.getRepository(Customer).findOneBy(actor.role==='ADMIN'?{id:customerId}:{id:customerId,salespersonId:actor.id});if(!customer)throw new NotFoundException('Customer not found');await this.expire();return this.query(actor).andWhere('quotation.customerId = :customerId',{customerId}).orderBy('quotation.createdAt','DESC').getMany()}
 async findOne(id:number,actor:Actor){await this.expire();const item=await this.query(actor).andWhere('quotation.id = :id',{id}).getOne();if(!item)throw new NotFoundException('Quotation not found');return item}
 async update(id:number,data:any,actor:Actor){return this.dataSource.transaction(async manager=>{const quotation=await this.one(id,actor,manager);if(!quotation)throw new NotFoundException('Quotation not found');if(quotation.status==='APPROVED')throw new BadRequestException('Approved quotation cannot be edited');const previousStatus=quotation.status;if(data.customerId!==undefined||data.userId!==undefined){const resolved=await this.owner(manager,data.customerId??quotation.customerId,data.userId??quotation.userId,actor);quotation.customerId=resolved.customer.id;quotation.userId=resolved.user.id}if(data.quotationNumber!==undefined){const number=String(data.quotationNumber).trim();if(!number)throw new BadRequestException('quotationNumber is required');const duplicate=await manager.getRepository(Quotation).createQueryBuilder('q').where('q.quotationNumber = :number AND q.id != :id',{number,id}).getOne();if(duplicate)throw new BadRequestException('quotationNumber already exists');quotation.quotationNumber=number}if(data.quotationDate!==undefined)quotation.quotationDate=this.validDate(data.quotationDate);if(data.description!==undefined)quotation.description=String(data.description||'').trim()||null;if(data.items!==undefined||data.discount!==undefined||data.tax!==undefined){const calculated=this.calculate({items:data.items??quotation.items,discount:data.discount??quotation.discount,tax:data.tax??quotation.tax});await manager.getRepository(QuotationItem).delete({quotationId:quotation.id});quotation.items=calculated.items.map(i=>manager.getRepository(QuotationItem).create(i));quotation.subtotal=calculated.subtotal;quotation.discount=calculated.discount;quotation.tax=calculated.tax;quotation.totalAmount=calculated.totalAmount}if(data.status!==undefined)quotation.status=this.validStatus(data.status);if(data.validUntil!==undefined)quotation.validUntil=data.validUntil?this.validDate(data.validUntil):null;await manager.getRepository(Quotation).save(quotation);const sent=data.status==='SENT'&&previousStatus!=='SENT';await this.history(manager,quotation.customerId,actor.id,sent?`Quotation Sent: ${quotation.quotationNumber}`:`Quotation Updated: ${quotation.quotationNumber}`,sent?'QUOTATION_SENT':'QUOTATION_UPDATED');return this.one(id,actor,manager)})}
 async remove(id:number,actor:Actor){const item=await this.findOne(id,actor);if(item.status==='APPROVED')throw new BadRequestException('Approved quotation cannot be deleted');await this.quotations.remove(item);return{deleted:true,id}}
 async approve(id:number,actor:Actor){return this.dataSource.transaction(async manager=>{const q=await this.one(id,actor,manager);if(!q)throw new NotFoundException('Quotation not found');if(!['DRAFT','SENT'].includes(q.status))throw new BadRequestException('Only draft or sent quotations can be approved');q.status='APPROVED';await manager.getRepository(Quotation).save(q);await this.history(manager,q.customerId,actor.id,`Quotation Approved: ${q.quotationNumber}`,'QUOTATION_APPROVED');return this.one(id,actor,manager)})}
 async convertToInvoice(id:number,actor:Actor){return this.dataSource.transaction(async manager=>{const q=await this.one(id,actor,manager);if(!q)throw new NotFoundException('Quotation not found');if(q.status!=='APPROVED')throw new BadRequestException('Only approved quotations can be converted');if(q.invoiceId)throw new BadRequestException('Quotation has already been converted');const items=q.items.map(({productName,quantity,unitPrice,discount,totalPrice})=>({productName,quantity,unitPrice,discount:Number(discount||0),totalPrice}));const invoice=await manager.getRepository(Invoice).save({customerId:q.customerId,userId:q.userId,invoiceNumber:this.number('INV'),total:q.totalAmount,items,status:'PENDING'});q.invoiceId=invoice.id;q.invoice=invoice;q.convertedAt=new Date();await manager.getRepository(Quotation).save(q);await this.history(manager,q.customerId,actor.id,`Quotation Converted To Invoice: ${q.quotationNumber} â†’ ${invoice.invoiceNumber}`,'QUOTATION_CONVERTED');return{quotation:await this.one(id,actor,manager),invoice}})}
 async reject(id:number,actor:Actor){const item=await this.findOne(id,actor);if(item.status==='APPROVED')throw new BadRequestException('Approved quotation cannot be rejected');item.status='REJECTED';await this.quotations.save(item);await this.dataSource.getRepository(CustomerHistory).save({customerId:item.customerId,userId:actor.id,action:'QUOTATION',description:`Quotation ${item.quotationNumber} rejected`});return this.findOne(id,actor)}
 private query(actor:Actor){return this.quotations.createQueryBuilder('quotation').leftJoinAndSelect('quotation.customer','customer').leftJoinAndSelect('quotation.user','user').leftJoinAndSelect('quotation.items','items').leftJoinAndSelect('quotation.invoice','invoice').where(actor.role==='ADMIN'?'1=1':'quotation.userId = :userId AND customer.salespersonId = :userId',{userId:actor.id})}
 private one(id:number,actor:Actor,manager:EntityManager){return manager.getRepository(Quotation).createQueryBuilder('quotation').leftJoinAndSelect('quotation.customer','customer').leftJoinAndSelect('quotation.user','user').leftJoinAndSelect('quotation.items','items').leftJoinAndSelect('quotation.invoice','invoice').where('quotation.id = :id',{id}).andWhere(actor.role==='ADMIN'?'1=1':'quotation.userId = :userId AND customer.salespersonId = :userId',{userId:actor.id}).getOne()}
 private async owner(manager:EntityManager,customerValue:unknown,userValue:unknown,actor:Actor){const customerId=this.validId(customerValue,'customerId');const customer=await manager.getRepository(Customer).findOneBy(actor.role==='ADMIN'?{id:customerId}:{id:customerId,salespersonId:actor.id});if(!customer)throw new BadRequestException('Customer not found or not accessible');const userId=actor.role==='SALES'?actor.id:Number(userValue||customer.salespersonId);const user=await manager.getRepository(User).findOneBy({id:userId,role:'SALES'});if(!user)throw new BadRequestException('A SALES user must be assigned');return{customer,user}}
 private calculate(data:any){if(!Array.isArray(data.items)||!data.items.length)throw new BadRequestException('At least one item is required');const items=data.items.map((raw:any,index:number)=>{const productName=String(raw.productName||'').trim(),quantity=Number(raw.quantity),unitPrice=Number(raw.unitPrice),discount=Number(raw.discount||0);if(!productName||!Number.isInteger(quantity)||quantity<1||!Number.isFinite(unitPrice)||unitPrice<0||!Number.isFinite(discount)||discount<0||discount>quantity*unitPrice)throw new BadRequestException(`Invalid item at row ${index+1}`);return{productName,quantity,unitPrice,discount,totalPrice:quantity*unitPrice-discount}});const subtotal=items.reduce((sum:number,item:any)=>sum+item.totalPrice,0),discount=Number(data.discount||0),tax=Number(data.tax||0);if(![discount,tax].every(v=>Number.isFinite(v)&&v>=0))throw new BadRequestException('discount and tax must be positive numbers');const totalAmount=subtotal-discount+tax;if(totalAmount<0)throw new BadRequestException('Total amount cannot be negative');return{items,subtotal,discount,tax,totalAmount}}
 private validStatus(value:unknown){if(!STATUSES.includes(value as QuotationStatus))throw new BadRequestException(`status must be one of ${STATUSES.join(', ')}`);return value as QuotationStatus} private validId(value:unknown,field:string){const id=Number(value);if(!Number.isInteger(id)||id<1)throw new BadRequestException(`${field} must be a positive integer`);return id} private validDate(value:unknown){const date=new Date(String(value));if(Number.isNaN(date.getTime()))throw new BadRequestException('validUntil is invalid');return date} private number(prefix:string){return`${prefix}-${new Date().toISOString().slice(0,10).replace(/-/g,'')}-${randomBytes(4).toString('hex').toUpperCase()}`} private history(manager:EntityManager,customerId:number,userId:number,description:string,action='QUOTATION_CREATED'){return manager.getRepository(CustomerHistory).save({customerId,userId,action,description})} private expire(){return this.quotations.createQueryBuilder().update().set({status:'EXPIRED'}).where('status IN (:...statuses) AND validUntil IS NOT NULL AND validUntil < :now',{statuses:['DRAFT','SENT'],now:new Date()}).execute()}
}
