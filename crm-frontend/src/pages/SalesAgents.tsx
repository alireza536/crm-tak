import { useEffect,useMemo,useState } from "react";
import { Award,BadgeCheck,BarChart3,Banknote,Crown,Medal,RefreshCw,ShoppingBag,Users } from "lucide-react";
import { Bar,BarChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";
import { ActionButton,Badge,ChartCard,EmptyState,GlassCard,PageHeader } from "../components/ui/DesignSystem";
import { getRoleDashboard } from "../services/api";
import "./SalesAgents.css";
type Agent={userId:number;name:string;salesCount:number;revenue:number|string};const fa=(v:unknown)=>Number(v||0).toLocaleString("fa-IR"),money=(v:unknown)=>`${fa(v)} ریال`;
export default function SalesAgents(){const [agents,setAgents]=useState<Agent[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=async()=>{try{setLoading(true);setError("");const d=await getRoleDashboard();setAgents(Array.isArray(d.salesPerformance)?d.salesPerformance:[])}catch(e){console.error(e);setError("اطلاعات کارشناسان دریافت نشد.")}finally{setLoading(false)}};useEffect(()=>{void load()},[]);const ranked=useMemo(()=>[...agents].sort((a,b)=>Number(b.revenue)-Number(a.revenue)),[agents]);const max=Math.max(...ranked.map(x=>Number(x.revenue)),0);return <main className="agentsPage executiveAgents" dir="rtl"><PageHeader eyebrow="SALES TEAM" title="کارشناسان فروش" description="ارزیابی عملکرد، رتبه‌بندی فروش و درآمد کارشناسان" actions={<ActionButton onClick={()=>void load()}><RefreshCw className={loading?"spin":""} size={15}/> بروزرسانی</ActionButton>}/>{error?<EmptyState title="دریافت اطلاعات ناموفق بود" description={error}/>:<><section className="agentGrid">{ranked.map((a,i)=><GlassCard className="agentCard" key={a.userId} style={{animationDelay:`${i*.07}s`}}><div className="agentCardTop"><Badge tone={i===0?"warning":"purple"}>{i===0?<><Crown size={11}/> رتبه اول</>:<>رتبه {fa(i+1)}</>}</Badge><span className="agentOnline">آنلاین<i/></span></div><div className="agentAvatar"><span>{a.name.charAt(0)||"؟"}</span><i/></div><h2>{a.name}</h2><p><BadgeCheck size={13}/> کارشناس فروش</p><div className="agentStats"><div><ShoppingBag/><span>تعداد فروش</span><strong>{fa(a.salesCount)}</strong></div><div><Banknote/><span>درآمد</span><strong>{money(a.revenue)}</strong></div><div><Users/><span>مشتریان</span><strong>—</strong></div><div><BarChart3/><span>نرخ تبدیل</span><strong>—</strong></div></div><footer><span>سهم از بیشترین درآمد</span><div><i style={{width:max?`${Math.max(5,Number(a.revenue)/max*100)}%`:"0%"}}/></div></footer></GlassCard>)}</section>{!loading&&!ranked.length&&<EmptyState title="کارشناسی برای نمایش وجود ندارد" description="API داشبورد داده عملکردی بازنگرداند."/>}<section className="agentsAnalytics"><ChartCard title="مقایسه عملکرد فروش" subtitle="درآمد کارشناسان"><div className="agentsChart">{ranked.length?<ResponsiveContainer width="100%" height="100%"><BarChart data={ranked} layout="vertical" margin={{left:15,right:10}}><defs><linearGradient id="agentBars" x1="0" y1="0" x2="1" y2="0"><stop offset="0" stopColor="#7c3aed"/><stop offset="55%" stopColor="#4f46e5"/><stop offset="100%" stopColor="#22d3ee"/></linearGradient></defs><CartesianGrid strokeDasharray="3 9" horizontal={false}/><XAxis type="number" hide/><YAxis type="category" dataKey="name" width={125} axisLine={false} tickLine={false}/><Tooltip formatter={v=>[money(v),"درآمد"]} cursor={{fill:"rgba(139,92,246,.055)",radius:10}} contentStyle={{background:"#11182b",border:"1px solid rgba(139,92,246,.3)",borderRadius:12,color:"#fff"}}/><Bar dataKey="revenue" fill="url(#agentBars)" radius={[9,9,9,9]} barSize={18} animationDuration={1100}/></BarChart></ResponsiveContainer>:<EmptyState title="داده‌ای برای نمودار وجود ندارد"/>}</div></ChartCard><GlassCard className="agentRanking"><header><Award/><div><span>LEADERBOARD</span><h2>رتبه‌بندی کارشناسان</h2></div></header><div>{ranked.map((a,i)=><article key={a.userId}><span className={`rankIcon rank${i+1}`}>{i<3?<Medal/>:fa(i+1)}</span><i>{a.name.charAt(0)}</i><div><strong>{a.name}</strong><small>{fa(a.salesCount)} فروش ثبت‌شده</small></div><b>{money(a.revenue)}</b></article>)}</div></GlassCard></section></>}</main>}
